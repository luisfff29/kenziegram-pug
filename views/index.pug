doctype html
html(lang='en')
  include layout/head.pug
  body.container
    div(class='row justify-content-center')
      div(class='col-xs-6 col-md-8 col-lg-6')
        h1= title
        form(action='/upload' method='POST' enctype='multipart/form-data' class='d-flex align-items-center p-4')
          input(type='file' class='form-control-file' name='myImage')
          button(type='submit' class='btn btn-primary btn-lg ml-auto') Upload
        each pic in pics
          div.p-2
            img(src='/uploads/' + pic class='img-fluid img-thumbnail' alt=pic)
    include layout/foot.pug
